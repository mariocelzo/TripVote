# Dev-only dependencies — non vanno nel container di produzione

-r requirements.txt

# Test
pytest==8.3.*
pytest-asyncio==0.24.*
pytest-cov==5.0.*
pytest-httpx==0.32.*
pytest-mock==3.14.*

# Lint & format
ruff==0.6.*

# Type checking (opzionale)
mypy==1.11.*

# Test factories
faker==28.*

# Pre-commit hooks
pre-commit==3.8.*
